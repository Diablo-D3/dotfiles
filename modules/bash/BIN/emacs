#!/usr/bin/env bash

if [ $TERM = "tmux*" ]; then
    export TERM=tmux-emacs
else
    export TERM=xterm-emacs
fi

if [[ ! "$@" == *"--no-site-file"* ]]; then
    exec /usr/bin/emacsclient -a '/usr/bin/emacs --daemon' -nw -c "$@"
else
    exec /usr/bin/emacs -nw "$@"
fi
